import {Link} from 'react-router-dom'; import {ArrowRight} from 'lucide-react'; import {collections} from '../data/demo'
export function Collections(){return <div className="page collections-page"><header className="page-head dark"><span>ARCHIVO DE EDICIONES</span><h1>Colecciones.</h1></header><div className="collection-list">{collections.map((c,i)=><Link style={{'--cover':`url(${c.image})`} as React.CSSProperties} to={`/colecciones/${c.slug}`} key={c.slug}><span>EDICIÓN / {c.edition}</span><h2>{c.name}</h2><p>{c.copy}</p><b>Ver colección <ArrowRight/></b><em>0{i+1}</em></Link>)}</div></div>}
