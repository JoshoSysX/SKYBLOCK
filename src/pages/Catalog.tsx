import {useMemo,useState} from 'react'; import {Search} from 'lucide-react'; import {products} from '../data/demo'; import {ProductCard} from '../components/ProductCard'
export function Catalog(){const[query,setQuery]=useState('');const[type,setType]=useState('Todos');const filtered=useMemo(()=>products.filter(p=>(type==='Todos'||p.type===type)&&p.name.toLowerCase().includes(query.toLowerCase())),[query,type]);return <div className="page light-page"><header className="page-head"><span>SKYBLOCK / ARCHIVE</span><h1>Catálogo.</h1><p>Piezas urbanas, series limitadas y básicos hechos para Tarapoto.</p></header><div className="filters"><label><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar por nombre"/></label>{['Todos','Camisetas','Sudaderas','Accesorios'].map(t=><button className={type===t?'active':''} onClick={()=>setType(t)} key={t}>{t}</button>)}</div>{filtered.length?<div className="product-grid">{filtered.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="empty"><h2>No encontramos piezas.</h2><p>Prueba con otro término o filtro.</p></div>}</div>}
