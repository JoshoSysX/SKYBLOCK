import {Link} from 'react-router-dom'; import type {products} from '../data/demo'; type Product=(typeof products)[number]
export function ProductCard({product}:{product:Product}){return <Link className="product-card" to={`/producto/${product.slug}`}><div className="image-wrap"><img src={product.image} alt={product.name} loading="lazy" width="800" height="1000"/>{product.limited&&<span className="badge">Edición limitada</span>}</div><div className="product-meta"><span>{product.type} / {product.collection}</span><h3>{product.name}</h3><b>S/ {product.price.toFixed(2)}</b></div></Link>}
